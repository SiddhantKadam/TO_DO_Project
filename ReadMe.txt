1. Open command promt and install packages using "npm install"
2. Install json server using "npm install -g json-server"
3. Start the angular project using "ng serve"
4. For the server, We are using here json server. To start the server, run "json-server --watch db.json"

The project is ready to work!

For further details regarding project is metioned in the documentation provide.

